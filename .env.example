# bripo — Altos de Maria construction dashboard
SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
SUPABASE_PROJECT_REF=YOUR_PROJECT_REF
# publishable key = anon role, RLS applies. Safe in the client HTML.
SUPABASE_PUBLISHABLE_KEY=sb_publishable_xxxxxxxxxxxx
# secret key: grab from Supabase dashboard -> Project Settings -> API keys if a
# server-side job is ever added. NOT needed for v1 (manual entry only).
SUPABASE_SECRET_KEY=
